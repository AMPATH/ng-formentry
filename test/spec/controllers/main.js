'use strict';

describe('Controller: MainCtrl', function () {

  // load the controller's module
  beforeEach(module('ngFormentryApp'));



  it('should check that false is false', function () {
    expect(false).toBe(false);
  });
});
